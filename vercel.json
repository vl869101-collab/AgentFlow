{
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @agentflow/shared build && pnpm --filter @agentflow/web build",
  "outputDirectory": "apps/web/.next"
}
